in the tmarkt csv file, 

the 'name' variable features the name of the player. 

the 'url' variable has a link to a picture of the player.

to run:
    open GuessFootballer in explorer
    shift + rightclick on open space -> run in Powershell
    type python3 -m http.server
    127.0.0.1:8000/home.html to run in chrome
    ctrl=+c to exit shell

TODO:
    - past clues should still be displayed when the guess is incorrect. 
    - allow for input without diacritics/accents/etc to still be valid